apiVersion: 1

providers:
  - name: "News-Signals"
    folder: "News-Signals"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
